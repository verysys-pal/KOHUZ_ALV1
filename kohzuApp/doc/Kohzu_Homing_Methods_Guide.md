# Kohzu ARIES 컨트롤러 원점 복귀(Homing) 방법 가이드

**작성일**: 2026-02-13 (v2.0 업데이트)  
**대상 시스템**: KOHUZ_ALV1 (Kohzu ARIES + XA07A-L202 스테이지)  
**참조 자료**: ARIES/LYNX 매뉴얼, NotebookLM 소스, KohzuAriesDriver.cpp  
**지원 Method**: 4 (NORG 표준), 7 (CW Limit), 8 (CCW Limit)  

---

## 목차

1. [개요](#1-개요)
2. [ARIES 컨트롤러 ORG 명령 사양](#2-aries-컨트롤러-org-명령-사양)
3. [원점 복귀 방법 15종 전체 목록](#3-원점-복귀-방법-15종-전체-목록)
4. [원점 복귀 관련 시스템 파라미터](#4-원점-복귀-관련-시스템-파라미터)
5. [원점 복귀 시퀀스 (Method 4/7/8)](#5-원점-복귀-시퀀스-method-478)
6. [프로젝트 구현 — Method 선택 기능](#6-프로젝트-구현--method-선택-기능)
7. [EPICS 연동 및 사용법](#7-epics-연동-및-사용법)
8. [트러블슈팅](#8-트러블슈팅)
9. [부록: 센서 배치도 및 용어 정리](#9-부록-센서-배치도-및-용어-정리)

---

## 1. 개요

### 1.1 원점 복귀(Homing)란?

**원점 복귀(Origin Return, Homing)**는 스테이지나 모터를 사전에 정의된 기준 위치(원점)로 이동시켜 **좌표계를 0으로 초기화**하는 과정입니다. 모든 후속 위치 제어의 기준점이 되므로, 시스템 시작 시 반드시 수행해야 합니다.

### 1.2 왜 원점 복귀가 필요한가?

| 목적 | 설명 |
|------|------|
| **좌표 기준 설정** | 전원 OFF/ON 후 모터 위치를 알 수 없으므로, 물리적 기준점에서 좌표를 초기화 |
| **재현성 확보** | 매번 동일한 위치에서 시작하여 실험/공정의 반복성 보장 |
| **안전 운전** | 소프트웨어 리미트가 정확한 좌표 기반으로 동작하도록 보장 |
| **탈조 복구** | 스테퍼 모터 탈조 시 위치 보정의 유일한 방법 |

### 1.3 ARIES 컨트롤러에서의 원점 복귀

Kohzu **ARIES**(Model 3) / **LYNX** 컨트롤러는 **`ORG` 명령**을 통해 원점 복귀를 수행하며, **시스템 파라미터 No.2 (ORG TYPE)**으로 총 **15가지 방법**을 지원합니다.

---

## 2. ARIES 컨트롤러 ORG 명령 사양

### 2.1 명령 포맷

```
ORG <축번호>/<속도테이블>/<응답방식>
```

| 파라미터 | 범위 | 설명 |
|----------|------|------|
| **a** (Axis No.) | 1 ~ 32 | 대상 축 번호 |
| **b** (Speed Table No.) | 0 ~ 9 | 센서까지 고속 접근 시 사용할 속도 테이블 번호 |
| **c** (Response Method) | 0, 1 | `0`=완료 후 응답 (Completion), `1`=즉시 응답 (Quick) |

### 2.2 응답 포맷

| 상태 | 응답 |
|------|------|
| **정상** | `C ORG <Axis No.>` |
| **에러** | `E ORG <Axis No.> <Error No.>` |

### 2.3 주요 에러 코드

| 에러 코드 | 의미 |
|-----------|------|
| `10n` | n번째 파라미터 범위 초과 |
| `200` | 해당 축이 존재하지 않음 |
| `3xx` | 원점 센서 관련 에러 |
| `600` | 축이 이미 동작 중 |

### 2.4 속도 설정 (WTB 명령)

ORG 명령 실행 전, **`WTB` 명령**으로 원점 복귀 시 사용할 속도 테이블을 설정합니다:

```
WTB <축>/<테이블>/<시작속도>/<최고속도>/<가속시간>/<감속시간>/<패턴>
```

| 파라미터 | 단위 | 설명 |
|----------|------|------|
| 시작속도 | pps | 기저 속도 (Start Speed) |
| 최고속도 | pps | 센서까지 접근하는 주행 속도 (Top Speed) |
| 가속시간 | ×10ms | 가속에 걸리는 시간 |
| 감속시간 | ×10ms | 감속에 걸리는 시간 |
| 패턴 | - | `1`=구형, `2`=사다리꼴, `3`=S자 |

> ⚠️ **주의**: 원점 센서 감지 후 **정밀 스캔 속도**는 WTB가 아닌 **System Parameter No.3 (ORG SCAN SPEED)**로 별도 설정합니다.

---

## 3. 원점 복귀 방법 15종 전체 목록

### 3.1 분류 개요

```
┌──────────────────────────────────────────────────────┐
│              ARIES ORG METHOD 분류                    │
├──────────────┬───────────────────────────────────────┤
│ 센서 기반     │ Method 1~9                            │
│ (기계적 원점) │ ORG, NORG, DATUM, Limit 센서 활용     │
├──────────────┼───────────────────────────────────────┤
│ 현재 위치     │ Method 10                             │
│ (이동 없음)   │ 현재 위치를 원점으로 설정              │
├──────────────┼───────────────────────────────────────┤
│ Z상 기반      │ Method 11~15                          │
│ (엔코더)     │ 엔코더 Z상 신호 활용                   │
└──────────────┴───────────────────────────────────────┘
```

### 3.2 전체 방법 상세

#### 🔵 센서 기반 방식 (Method 1~9)

| Method | 사용 센서 | 원점 기준 | 설명 |
|--------|-----------|-----------|------|
| **1** | DATUM(Zone) + ORG | ORG 에지 | DATUM 영역 내에서 첫 번째 ORG 센서 에지 검출 |
| **2** | DATUM(Zone) | DATUM 에지 | DATUM 센서의 에지를 원점으로 설정 |
| **3** | NORG + ORG | ORG 에지 | NORG 범위 내의 ORG 센서 에지 검출 (정밀형) |
| **★4** | **NORG** | **NORG 에지** | **NORG 센서의 에지를 원점 (Kohzu 표준 방식)** |
| **5** | CW Limit + ORG | ORG 에지 | CW 리미트 근처의 ORG 센서 검출 |
| **6** | CCW Limit + ORG | ORG 에지 | CCW 리미트 근처의 ORG 센서 검출 |
| **7** | CW Limit | Limit 에지 | CW 리미트의 에지를 원점으로 설정 |
| **8** | CCW Limit | Limit 에지 | CCW 리미트의 에지를 원점으로 설정 |
| **9** | ORG | ORG 에지 | ORG 센서의 에지만으로 원점 설정 |

#### 🟢 현재 위치 방식 (Method 10)

| Method | 사용 센서 | 원점 기준 | 설명 |
|--------|-----------|-----------|------|
| **10** | (없음) | 현재 위치 | 구동 없이 현재 위치를 원점(0)으로 설정. ORG OFFSET 무효 |

#### 🟣 엔코더 Z상 방식 (Method 11~15)

| Method | 사용 센서 | 원점 기준 | 설명 |
|--------|-----------|-----------|------|
| **11** | 엔코더 Z상 | Z상 에지 | 이동 범위 내 첫 번째 Z상 신호 검출 |
| **12** | DATUM + Z상 | Z상 에지 | DATUM 내에서 Z상 에지 검출 |
| **13** | NORG + Z상 | Z상 에지 | NORG 범위 내 Z상 에지 검출 |
| **14** | CW Limit + Z상 | Z상 에지 | CW 리미트 근처의 Z상 에지 검출 |
| **15** | CCW Limit + Z상 | Z상 에지 | CCW 리미트 근처의 Z상 에지 검출 |

### 3.3 방법 선택 가이드

```
                    원점 복귀 방법 선택 플로차트
                    
                    엔코더(Z상) 사용?
                    ├── Yes → 센서 조합?
                    │         ├── NORG 있음 → Method 13
                    │         ├── DATUM 있음 → Method 12
                    │         ├── Limit만 → Method 14/15
                    │         └── 없음 → Method 11
                    │
                    └── No → ORG 센서 있음?
                              ├── Yes → NORG도 있음?
                              │         ├── Yes → Method 3 (정밀)
                              │         └── No → Method 9
                              │
                              └── No → NORG만 있음?
                                        ├── Yes → ★ Method 4 (표준)
                                        └── No → Limit 사용
                                                  ├── CW → Method 7
                                                  └── CCW → Method 8
```

---

## 4. 원점 복귀 관련 시스템 파라미터

ARIES 컨트롤러의 **시스템 파라미터(System Parameter)**는 전원을 꺼도 유지되며, 원점 복귀 동작의 세부 사항을 제어합니다.

### 4.1 핵심 파라미터 일람

| No. | 파라미터명 | 설정 범위 | 기본값 | 설명 |
|-----|-----------|-----------|--------|------|
| **1** | **ORG OFFSET** | -134,217,728 ~ +134,217,727 | 0 | 원점 센서 검출 후 추가 이동량(pulse) |
| **2** | **ORG TYPE** | 1 ~ 15 | 4 | 원점 복귀 방법 선택 |
| **3** | **ORG SCAN SPEED** | 1 ~ 5,000,000 pps | 500 | 원점 정밀 검색 속도 |
| **7** | **PM ROTATE CHANGE** | 0 / 1 | 0 | CW/CCW 방향 반전 (0=정상, 1=반전) |

### 4.2 각 파라미터 상세

#### System No.1: ORG OFFSET (원점 오프셋)

센서 에지를 검출한 후, 설정된 **펄스 수만큼 추가 이동**한 위치를 원점(0)으로 합니다.

```
센서 에지 검출 위치
      │
      ▼
  ────┼────────────┼──── 이동 방향 →
      │  ORG OFFSET │
      │◄────────────►│
                     │
                     ▼
                 여기를 0으로 설정
```

- **양수**: CW 방향으로 추가 이동
- **음수**: CCW 방향으로 추가 이동
- **0**: 센서 에지 위치를 그대로 원점으로 설정
- **※ Method 10에서는 무효**

#### System No.2: ORG TYPE (원점 복귀 방법)

[3장](#3-원점-복귀-방법-15종-전체-목록) 참조. 기본값 **4 (NORG 표준 방식)**.

#### System No.3: ORG SCAN SPEED (원점 스캔 속도)

고속으로 센서 근처까지 접근한 후, **정밀하게 에지를 찾을 때**의 저속 이동 속도(Creep Speed)입니다.

```
속도
 ▲
 │  ┌──────────────┐ ← WTB Top Speed (고속 접근)
 │  │              │
 │  │              │ ← NORG 감지: 감속
 │  │              ├──┐
 │  │              │  └──┐
 │  │              │     └─────── ← ORG SCAN SPEED (정밀 검색)
 │──┘              │              
 └─────────────────┴──────────────── 시간 →
```

- 값이 **작을수록** 정밀하지만, 시간이 오래 걸림
- 값이 **클수록** 빠르지만, 정밀도가 떨어짐
- **권장**: 100 ~ 1000 pps (정밀 스테이지의 경우)

#### System No.7: PM ROTATE CHANGE (방향 반전)

CW/CCW의 물리적 방향을 반전시킵니다:
- `0`: 정상 (기본)
- `1`: CW↔CCW 반전

### 4.3 원점 복귀 방향 결정 로직

ARIES 컨트롤러는 **별도의 복귀 방향 파라미터가 없습니다**. 선택한 Method와 현재 센서 상태에 따라 **자동으로 방향을 판단**합니다.

- **Method 4 (표준)**: 기본적으로 CCW 방향으로 NORG 센서를 찾으러 이동
  - 이미 NORG 위에 있으면: 일단 벗어난 후 재접근
  - CCW 리미트에 도달하면: 자동 반전하여 CW 방향으로 재시도

### 4.4 좌표 초기화 시퀀스

```
1. ORG 명령 수신
       ↓
2. 설정된 Method에 따라 센서 검출
       ↓
3. 센서 에지에서 정지
       ↓
4. ORG OFFSET 만큼 추가 이동
       ↓
5. 최종 위치의 내부 좌표 카운터를 '0'으로 리셋
       ↓
6. 응답 전송: "C ORG <axis>"
```

---

## 5. 원점 복귀 시퀀스 (Method 4/7/8)

### 5.1 Method 4 — NORG 센서 에지 (Kohzu 표준)

가장 많이 사용되는 **Method 4 (NORG 센서 에지 검출)**의 동작 순서:

```
      CW 리미트                NORG 센서              CCW 리미트
         │                   ┌────┐                      │
─────────┤                   │    │                      ├─────────
         │                   │    │                      │
         │    ① 고속 CCW →   │    │                      │
         │    ─────────────→ │    │                      │
         │                   │ ②  │ NORG 감지 → 감속정지  │
         │                   │  ──┤                      │
         │                   │    │                      │
         │    ③ 반전 CW ←    │    │                      │
         │    ←──── 저속 ────│    │                      │
         │         (SCAN SPEED)   │                      │
         │                   ├────│                      │
         │                   │ ④ NORG 에지 검출 → 정지    │
         │                   │    │                      │
         │                   │    ├── ⑤ ORG OFFSET 이동   │
         │                   │    │   └── ⑥ 좌표=0 설정   │
```

| 단계 | 동작 | 속도 | 설명 |
|------|------|------|------|
| ① | CCW 방향 고속 이동 | WTB Top Speed | 설정된 속도 테이블의 최고 속도로 센서 방향 이동 |
| ② | NORG 감지 → 감속 정지 | → 0 | NORG 센서를 통과하면 감속하여 정지 |
| ③ | CW 방향 저속 반전 | ORG SCAN SPEED | 반대 방향으로 저속 이동하며 에지 검색 |
| ④ | NORG 에지 검출 | → 0 | 센서 OFF→ON 또는 ON→OFF 에지에서 즉시 정지 |
| ⑤ | ORG OFFSET 이동 | WTB Speed | OFFSET이 0이 아니면 설정량만큼 추가 이동 |
| ⑥ | 좌표 초기화 | - | 현재 위치 카운터를 0으로 리셋 |

### 5.2 Method 7 — CW Limit 에지

별도의 원점 센서가 없을 때, **CW 리미트 스위치(L+)의 에지**를 원점으로 사용합니다.

```
      CW 리미트 (L+)                                   CCW 리미트
         │                                                │
─────────┤                                                ├─────────
         │                                                │
         │ ← ① 고속 CW ──────────────────────────────────│
         │                                                │
    ② CW Limit 감지 → 감속                                │
    ┤ ──┐                                                 │
    │   │                                                 │
    │   │ ③ 반전 CCW → 저속 (SCAN SPEED)                   │
    │   ├── → ────                                        │
    │   │                                                 │
    ├───│ ④ CW Limit 에지 검출 → 정지                      │
    │   │                                                 │
    │   ├── ⑤ ORG OFFSET 이동                              │
    │   │   └── ⑥ 좌표=0 설정                              │
```

| 단계 | 동작 | 설명 |
|------|------|------|
| ① | CW 방향 고속 이동 | WTB Top Speed로 CW 리미트 방향 이동 |
| ② | CW Limit 감지 → 감속 | 리미트 스위치 도달 시 자동 감속 정지 |
| ③ | CCW 방향 저속 반전 | ORG SCAN SPEED로 에지 정밀 검색 |
| ④ | Limit 에지 검출 | ON→OFF 에지에서 정지 |
| ⑤⑥ | OFFSET + 좌표 초기화 | 센서 에지 + OFFSET 위치를 0으로 설정 |

> **특징**: NORG 센서가 없는 시스템에서 사용. 원점 = 스트로크의 CW 끝.

### 5.3 Method 8 — CCW Limit 에지

Method 7의 반대 방향 버전. **CCW 리미트 스위치(L-)의 에지**를 원점으로 사용합니다.

```
      CW 리미트                                    CCW 리미트 (L-)
         │                                                │
─────────┤                                                ├─────────
         │                                                │
         │──────────────────────── ① 고속 CCW → ──────────│
         │                                                │
         │                               ② CCW Limit 감지 → 감속
         │                                           ──┤ ┤
         │                                             │ │
         │                  ③ 반전 CW ← 저속 ──────────│ │
         │                                         ────├ │
         │                                             │ │
         │                      ④ CCW Limit 에지 검출 → ├─┤
         │                                             │
         │                                  ⑤ OFFSET   │
         │                                  ⑥ 좌표=0   │
```

| 단계 | 동작 | 설명 |
|------|------|------|
| ① | CCW 방향 고속 이동 | WTB Top Speed로 CCW 리미트 방향 이동 |
| ② | CCW Limit 감지 → 감속 | 리미트 스위치 도달 시 자동 감속 정지 |
| ③ | CW 방향 저속 반전 | ORG SCAN SPEED로 에지 정밀 검색 |
| ④ | Limit 에지 검출 | ON→OFF 에지에서 정지 |
| ⑤⑥ | OFFSET + 좌표 초기화 | 센서 에지 + OFFSET 위치를 0으로 설정 |

> **특징**: 원점 = 스트로크의 CCW 끝. Method 7과 방향만 반대.

### 5.4 Method 비교표

| 항목 | Method 4 (NORG) | Method 7 (CW Limit) | Method 8 (CCW Limit) |
|------|-----------------|---------------------|----------------------|
| **원점 기준** | NORG 센서 에지 | CW Limit 에지 | CCW Limit 에지 |
| **필요 센서** | NORG 필수 | CW Limit만 | CCW Limit만 |
| **원점 위치** | 스트로크 중간 | 스트로크 CW 끝 | 스트로크 CCW 끝 |
| **정밀도** | 높음 (전용 센서) | 보통 (리미트 겸용) | 보통 (리미트 겸용) |
| **추천 용도** | 정밀 위치 제어 | 센서 미장착 시 | 센서 미장착 시 |

---

## 6. 프로젝트 구현 — Method 선택 기능

### 6.1 아키텍처 개요

사용자가 **EPICS PV를 통해 원점 복귀 방법을 선택**할 수 있는 구조입니다.

```
┌─────────────────┐   caput    ┌─────────────────────────┐
│  EPICS OPI      │ ─────────→ │ KOHZU:m1:HomingMethod   │
│  (사용자 선택)   │            │  mbbo: NORG/CW_LIM/     │
│                 │            │        CCW_LIM           │
└─────────────────┘            └───────────┬─────────────┘
                                           │ asyn 파라미터
                                           ▼
                               ┌───────────────────────┐
                               │  KohzuAriesDriver      │
                               │  home() 함수            │
                               ├───────────────────────┤
                               │ ① PV에서 Method 읽기    │
                               │ ② WSY → ORG TYPE 변경   │
                               │ ③ WTB → 속도 설정       │
                               │ ④ ORG → 원점 복귀 실행   │
                               └───────────┬───────────┘
                                           │
                                           ▼
                               ┌───────────────────────┐
                               │  ARIES 컨트롤러        │
                               │  Method 4/7/8 자동 실행 │
                               └───────────────────────┘
```

### 6.2 지원하는 원점 복귀 방법

| PV 값 | ORG TYPE | 원점 기준 | 설명 |
|-------|----------|-----------|------|
| **`NORG`** (기본) | **4** | NORG 센서 에지 | Kohzu 표준. NORG 센서 필요 |
| **`CW_LIM`** | **7** | CW Limit 에지 | CW 리미트 스위치 활용 |
| **`CCW_LIM`** | **8** | CCW Limit 에지 | CCW 리미트 스위치 활용 |

### 6.3 구현 핵심 — WSY 명령을 통한 ORG TYPE 동적 변경

ARIES의 ORG TYPE은 **System Parameter No.2**로 관리됩니다. 드라이버는 `home()` 실행 시 **`WSY` 명령**을 전송하여 컨트롤러의 ORG TYPE을 실시간으로 변경합니다.

```
WSY 명령 포맷: WSY <축>/<시스템번호>/<설정값>
  예: WSY1/2/7  → 1번 축의 System No.2를 Method 7로 변경

응답:
  정상: "C WSY <axis> <sysNo>"
  에러: "E WSY <axis> <errNo>"
```

### 6.4 `home()` 함수 실행 흐름

```cpp
asynStatus KohzuAriesAxis::home(...) {
  // 1. 사용자가 선택한 Method 읽기 (EPICS PV)
  int method = 4;  // 기본값
  pC_->getIntegerParam(axisNo_, pC_->homingMethod_, &method);

  // 2. 유효성 검증 (4, 7, 8만 허용)
  //    잘못된 값 → Method 4로 폴백

  // 3. WSY 명령으로 컨트롤러 ORG TYPE 변경
  setSystemParam(2, method);  // WSY<axis>/2/<method>

  // 4. WTB로 접근 속도 설정
  setSpeedTable(minVelocity, maxVelocity, acceleration);

  // 5. ORG 명령 전송 (즉시 응답)
  sprintf(command, "ORG%d/0/1", axisID);
  pC_->sendCommand(command, ...);

  // 6. homingActive_ 플래그 → poll()에서 완료 감지
  homingActive_ = true;
}
```

### 6.5 파일 구조

| 파일 | 역할 |
|------|------|
| `kohzuApp/src/KohzuAriesDriver.h` | `homingMethod_` asyn 파라미터, `setSystemParam()` 선언 |
| `kohzuApp/src/KohzuAriesDriver.cpp` | WSY 명령 헬퍼, Method 선택 로직, homing 상태 추적 |
| `db/KOHZU_HomingMethod.db` | mbbo/mbbi 레코드 (사용자 선택 PV) |
| `iocBoot/iocKOHUZ_ALV1/motor.substitutions` | DB 로드 설정 |

### 6.6 motorStatusHomed_ 로직

| 항목 | `motorStatusAtHome_` | `motorStatusHomed_` |
|------|---------------------|---------------------|
| **의미** | 현재 물리적으로 원점 위치인지 | 원점 복귀를 수행했는지 |
| **소스** | STR `d` 필드 (실시간 센서) | `homed_` 소프트웨어 래치 |
| **원점 이탈 시** | OFF (0) | **유지 (1)** |
| **전원 OFF** | 초기화 | 초기화 |

### 6.7 리팩토링 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v0.1 | 2026-02-05 | 초기 구현: `ORG%d/0/0/0` (4개 인수, 잘못된 포맷) |
| v1.0 | 2026-02-13 | `ORG%d/0/1` (3개 인수)로 수정, WTB 속도 설정 추가 |
| v1.1 | 2026-02-13 | WTB 에러 105 해결, acceleration 단위 수정 |
| **v2.0** | **2026-02-13** | **Method 4/7/8 선택 기능, WSY 명령, HomingMethod PV 추가** |

---

## 7. EPICS 연동 및 사용법

### 7.1 원점 복귀 방법 선택 PV

| PV 이름 | 타입 | 설명 |
|---------|------|------|
| `KOHZU:m1:HomingMethod` | mbbo | 원점 복귀 방법 설정 (NORG / CW_LIM / CCW_LIM) |
| `KOHZU:m1:HomingMethod_RBV` | mbbi | 현재 설정값 읽기 (Readback) |

### 7.2 사용 방법

```bash
# ═══════════════════════════════════════════════════
# Step 1: 원점 복귀 방법 선택
# ═══════════════════════════════════════════════════

# Method 4 (NORG 센서 에지) — 기본값
caput KOHZU:m1:HomingMethod "NORG"

# Method 7 (CW Limit 에지)
caput KOHZU:m1:HomingMethod "CW_LIM"

# Method 8 (CCW Limit 에지)
caput KOHZU:m1:HomingMethod "CCW_LIM"

# 설정 확인
caget KOHZU:m1:HomingMethod_RBV   # → NORG / CW_LIM / CCW_LIM

# ═══════════════════════════════════════════════════
# Step 2: 원점 복귀 실행
# ═══════════════════════════════════════════════════

# HOMF 또는 HOMR (ARIES는 방향을 자동 판단하므로 동일 동작)
caput KOHZU:m1.HOMF 1

# ═══════════════════════════════════════════════════
# Step 3: 결과 확인
# ═══════════════════════════════════════════════════

caget KOHZU:m1.RBV     # → 0.000 (또는 ORG OFFSET 반영값)
caget KOHZU:m1.MSTA    # → Bit 14 (RA_HOMED) = 1
caget KOHZU:m1.DMOV    # → 1 (이동 완료)
```

### 7.3 Motor Record 관련 필드

| EPICS 필드 | MSTA Bit | 설명 | 드라이버 매핑 |
|------------|----------|------|---------------|
| **HOMF / HOMR** | - | 원점 복귀 실행 | → `home()` → WSY + ORG |
| **MSTA** Bit 7 | `EA_HOME` | Home 센서 활성 (실시간) | STR `d` ≠ 0 |
| **MSTA** Bit 14 | `RA_HOMED` | 원점 복귀 완료 (래치) | `homed_` 플래그 |

> ⚠️ **참고**: `forwards` 파라미터(HOMF=1, HOMR=0)는 무시됩니다.  
> ARIES 컨트롤러가 선택된 Method에 따라 복귀 방향을 자동으로 판단합니다.

### 7.4 검증 스크립트에서의 원점 복귀 테스트

```bash
# 검증 전 Method 확인
caget KOHZU:m1:HomingMethod_RBV

# 원점 복귀 실행
caput -w 60 KOHZU:m1.HOMF 1

# 검증 기준
# - DMOV = 1 (이동 완료)
# - RBV ≈ 0 (허용 오차 ±0.1)
# - MSTA Bit 14 = 1 (Homed)
```

---

## 8. 트러블슈팅

### 8.1 원점 복귀 실패 시 체크리스트

| 증상 | 원인 | 해결 방법 |
|------|------|-----------|
| `E ORG <axis> 3xx` | 원점 센서 미감지 | 센서 배선 확인, Method 설정 확인 |
| `E ORG <axis> 600` | 축이 이미 동작 중 | STP로 정지 후 재실행 |
| `E WSY <axis> <err>` | System Parameter 설정 실패 | 파라미터 범위 확인 (Method 1~15) |
| Home 후 위치 ≠ 0 | OFF (Offset) 설정됨 | `caget KOHZU:m1.OFF` 확인, 필요 시 0으로 리셋 |
| Home 타임아웃 | 센서까지 거리가 멀거나 속도 느림 | WTB Top Speed 증가, HOMF 타임아웃 연장 |
| 반복 시 위치 편차 | SCAN SPEED 과대 | System No.3 값 감소 (100~500 pps) |
| Method 7에서 실패 | CW Limit 미검출 | 리미트 센서 배선/극성 확인 |
| Method 8에서 실패 | CCW Limit 미검출 | 리미트 센서 배선/극성 확인 |
| HomingMethod PV 변경 안됨 | DB 미로드 | `motor.substitutions`에 `KOHZU_HomingMethod.db` 확인 |

### 8.2 Method 선택 확인

```bash
# 현재 설정된 Method 확인 (EPICS PV)
caget KOHZU:m1:HomingMethod_RBV

# 컨트롤러에 실제 설정된 ORG TYPE 확인 (RSY 명령)
# IOC 쉘에서 수동 전송:
# RSY1/2 → "C RSY 1 2 <값>"  (값 = 현재 ORG TYPE)
```

### 8.3 센서 상태 모니터링

```bash
# STR 명령으로 실시간 센서 상태 확인
# 응답: "C STR<axis> <b> <c> <d> <e> <f> <g>"
#   d = ORG/NORG 센서 비트마스크
#   e = CW/CCW 리미트 비트마스크

# EPICS에서 확인
caget KOHZU:m1.MSTA

# MSTA 비트 해석
#   Bit 3  (0x0008): Home Limit Switch (RA_HOME)
#   Bit 7  (0x0080): Home Switch Active (EA_HOME)
#   Bit 14 (0x4000): Homed (RA_HOMED)
```

### 8.4 ASYN 디버그 활성화

```bash
# IOC 쉘에서 실행
asynSetTraceMask("L0", 0, 255)
asynSetTraceIOMask("L0", 0, 255)

# → WSY, WTB, ORG 명령 송수신 전체 로그 확인 가능
# 확인 후 비활성화:
asynSetTraceMask("L0", 0, 1)
```

---

## 9. 부록: 센서 배치도 및 용어 정리

### 9.1 XA07A-L202 센서 배치도

```
      CCW Limit          NORG 센서           CW Limit
         │              ┌──────┐              │
─────────┤    ←  -35mm  │      │  +35mm →     ├──────────
         │              │      │              │
    리미트 센서          │ 원점  │          리미트 센서
    (L-)                │ 근접  │          (L+)
                        └──────┘
                    
                  ◄── 스트로크 ±35mm ──►
                     (총 70mm)
```

### 9.2 센서 용어 정리

| 용어 | 영문 | 설명 |
|------|------|------|
| **ORG** | Origin | 원점 센서. 고정밀 원점 위치를 제공하는 별도 센서 |
| **NORG** | Near Origin | 원점 근접 센서. 원점 부근에서 감속을 유도하는 센서 |
| **DATUM** | Datum / Zone | 영역 센서. 원점이 존재하는 영역을 표시 |
| **CW Limit** | Clockwise Limit (L+) | 정방향 이동 한계 센서 |
| **CCW Limit** | Counter-CW Limit (L-) | 역방향 이동 한계 센서 |
| **Z상** | Z-Phase / Index | 엔코더의 1회전당 1펄스 기준 신호 |
| **에지(Edge)** | Edge | 센서 신호의 ON↔OFF 전환점 |

### 9.3 ARIES 상태 응답 (STR) ORG 필드 해석

| d 값 | ORG 센서 | NORG 센서 | 의미 |
|------|----------|-----------|------|
| 0 | OFF | OFF | 원점 영역 밖 |
| 1 | OFF | **ON** | NORG 영역 내 (ORG 미감지) |
| 2 | **ON** | OFF | ORG 감지 (NORG 미감지) |
| 3 | **ON** | **ON** | ORG + NORG 모두 감지 |

---

## 변경 이력

| 일자 | 버전 | 변경 내용 |
|------|------|-----------|
| 2026-02-13 | v1.0 | 초판 작성 (NotebookLM 조회 + 소스 코드 분석 기반) |
| **2026-02-13** | **v2.0** | **Method 7/8 구현, WSY 동적 변경, HomingMethod PV 추가, 가이드 전면 개편** |

---

*본 문서는 NotebookLM(Kohzu Motor Integration and EPICS Control Systems Guide)의 ARIES 매뉴얼 소스와 `KohzuAriesDriver.cpp` 소스 코드를 기반으로 작성되었습니다.*
